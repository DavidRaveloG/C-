#include<isotream>
#include"listas.h"
using namespace std;
struct tarjetas{ int color, dia, mes, anio;
			String motivo;
};
struct jugador{Strign nombre, posicion;
	int edad;
	Lista <targetas> t;
};
struct equipo{String nombre, entrenador, region;
	Lista <jugador> j;
};
int main(){
	//listas de enteros se autollene, de reales y de caracteres 
}
